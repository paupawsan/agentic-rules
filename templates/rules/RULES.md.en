# {{display_name}} Agent Integration (English)

<!-- SAFETY_PRECAUTION_START -->
**⚠️ CRITICAL TEMPLATE PROTECTION**: This file is a TEMPLATE ONLY. Agents MUST NOT auto-load, auto-read, or auto-process this file under any circumstances. This file is NEVER executed directly.

**VALID USAGE**: Only load this template when explicitly renamed to one of: `AGENTS.md`, `GEMINI.md`, or `CLAUDE.md` AFTER explicit user activation AND when `{{plugin_key}}.enabled = true` in settings.json.

**INVALID USAGE**: Do not load this file automatically, do not reference it in startup processes, do not include it in any automated framework initialization.
<!-- SAFETY_PRECAUTION_END -->

**FRAMEWORK ENHANCEMENT**: When {{plugin_key}}.enabled = true in settings.json, agents can implement these algorithms for enhanced behavior.

## {{display_name}} Overview
{{description}}

## Required Implementation Algorithms

### Algorithm: {{pascal_case_name}}_Initialization_Process
**AVAILABLE**: When {{plugin_key}}.enabled = true

**Steps**:
1. Read modules/{{plugin_name}}/settings.json
2. Initialize {{plugin_name_kebab}} system
3. Validate configuration settings
4. Return initialized system

**CRITICAL**: Do NOT auto-enable - respect user's enabled setting.

### Algorithm: {{pascal_case_name}}_Main_Process
**AVAILABLE**: Can execute for relevant user interactions when enabled

**Steps**:
1. Check {{plugin_key}}.enabled = true
2. Analyze current context and request
3. Apply {{plugin_name_kebab}} algorithms
4. Process and return results

### Algorithm: {{pascal_case_name}}_Cleanup_Process
**AVAILABLE**: Can execute periodically with user consent

**Steps**:
1. Check cleanup settings
2. Identify obsolete data
3. Request user consent for cleanup
4. Perform safe cleanup operations

## Configuration Requirements
- Settings file: `modules/{{plugin_name}}/settings.json`
- Configuration key: `{{plugin_key}}.enabled`
- Bootstrap compliance required

## Error Handling
**RECOMMENDED**: Agents can handle errors gracefully when using framework features:
- Configuration file missing → Skip {{plugin_name_kebab}} operations
- Invalid settings → Use default values and log warnings
- System errors → Continue operation without {{plugin_name_kebab}} features

## Framework Integration
**FRAMEWORK DESIGN**: This framework is designed as a tool to enhance agent behavior. Framework components remain separate from user project codebases.

## Agent Enhancement
Agents using this framework can benefit from:
- Respecting user settings and configurations when features are enabled
- Providing transparent feedback about framework status
- Maintaining natural communication patterns without exposing internal mechanics
- Following framework guidance seamlessly in the background

**FRAMEWORK BENEFITS**: Improved agent behavior through optional enhancements that don't interfere with natural user interactions.

<!-- METADATA: {{display_name}} agent integration template -->
<!-- LICENSE: Copyright (c) {{current_year}} {{author_name}} - Licensed under the MIT License. See LICENSE file for details. -->
