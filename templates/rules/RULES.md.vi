# {{display_name}} Tích hợp Đại lý (Tiếng Việt)

**⚠️ CẢNH BÁO AN TOÀN**: Đây là tệp TEMPLATE. Các đại lý KHÔNG được tải tệp này tự động. Chỉ tải khi đổi tên thành `AGENTS.md`, `GEMINI.md` hoặc `CLAUDE.md` sau khi kích hoạt người dùng rõ ràng VÀ khi `{{plugin_key}}.enabled = true` trong settings.json.

**BẮT BUỘC**: Nếu {{plugin_key}}.enabled = true trong settings.json, các đại lý PHẢI triển khai tất cả các thuật toán dưới đây.

## Tổng quan {{display_name}}
{{description}}

## Thuật toán Triển khai Bắt buộc

### Thuật toán: {{pascal_case_name}}_Initialization_Process
**BẮT BUỘC**: Thực hiện khi {{plugin_key}}.enabled = true

**Các bước**:
1. Đọc modules/{{plugin_name}}/settings.json
2. Khởi tạo hệ thống {{plugin_name_kebab}}
3. Xác thực cài đặt cấu hình
4. Trả về hệ thống đã khởi tạo

**QUAN TRỌNG**: KHÔNG kích hoạt tự động - tôn trọng cài đặt của người dùng.

### Thuật toán: {{pascal_case_name}}_Main_Process
**BẮT BUỘC**: Thực hiện cho tất cả tương tác người dùng có liên quan khi được kích hoạt

**Các bước**:
1. Kiểm tra {{plugin_key}}.enabled = true
2. Phân tích ngữ cảnh và yêu cầu hiện tại
3. Áp dụng thuật toán {{plugin_name_kebab}}
4. Xử lý và trả về kết quả

### Thuật toán: {{pascal_case_name}}_Cleanup_Process
**BẮT BUỘC**: Thực hiện định kỳ, yêu cầu sự đồng ý của người dùng

**Các bước**:
1. Kiểm tra cài đặt dọn dẹp
2. Xác định dữ liệu lỗi thời
3. Yêu cầu sự đồng ý của người dùng để dọn dẹp
4. Thực hiện các hoạt động dọn dẹp an toàn

## Yêu cầu Cấu hình
- Tệp cài đặt: `modules/{{plugin_name}}/settings.json`
- Khóa cấu hình: `{{plugin_key}}.enabled`
- Tuân thủ bootstrap bắt buộc

## Xử lý Lỗi
**BẮT BUỘC**: Các đại lý phải xử lý lỗi đúng cách:
- Không tìm thấy tệp cấu hình → Bỏ qua các hoạt động {{plugin_name_kebab}}
- Cài đặt không hợp lệ → Sử dụng giá trị mặc định và ghi nhật ký cảnh báo
- Lỗi hệ thống → Tiếp tục mà không có chức năng {{plugin_name_kebab}}

## Tích hợp Khung
**BẮT BUỘC**: Khung này vẫn là một công cụ và không được bao gồm trong cơ sở mã dự án người dùng. Tất cả tích hợp diễn ra thông qua các tệp quy tắc trong thư mục tương ứng của chúng.

## Trách nhiệm Đại lý
Các đại lý sử dụng khung này phải:
- Tôn trọng tất cả cài đặt và cấu hình của người dùng
- Cung cấp phản hồi rõ ràng khi {{plugin_name_kebab}} bị tắt
- Duy trì tính trung lập của khung trong nội dung được tạo
- Tuân thủ các yêu cầu tuân thủ bootstrap

**VI PHẠM**: Các đại lý không tôn trọng các yêu cầu tích hợp này hoặc cài đặt người dùng.

<!-- METADATA: {{display_name}} tích hợp đại lý template -->
<!-- LICENSE: Copyright (c) {{current_year}} {{author_name}} - Được cấp phép theo Giấy phép MIT. Xem tệp LICENSE để biết chi tiết. -->
